package jOptionPane;

/* import javax.swing (pacote de expansão do JOptionPane)
 Todo e qualquer comando que use o JOptionPane terá seu nome iniciado por "J"
 
 - "JOptionPane.showInputDialog("...") é o comando para a entrada de dados via OptionPane
 
 - "JOptionPane.showMessageDialog (null, "...");" é o comando necessário para mostrar um dado via OptionPane 
 
 "Integer"/"Character" & ".parseInt"/".parseChar" respectivamente servem para converter um dado Wrapper em um tipo primitivo via OptionPane
 Os outros modos de conversão seguem o modelo convencional usando seu primitivo EQUIVALENTE... 
 
 tabela wrapper || tabela primitivos	|| Conversão Wrapper para primitivo
 Integer		|| int					|| Integer.parseInt();
 Character		|| char					|| Character.parseChar();
 Boolean		|| boolean				|| Boolean.parseBoolean();
 Byte			|| byte					|| Byte.parseByte();
 Short			|| short				|| Short.parseShort();
 Long			|| long					|| Long.parseLong();
 Float			|| float				|| Float.ParseFloat();
 Double			|| double				|| Double.parseDouble();
 
 String não é considerado tipo primitivo, por isso nao entra nessa lista */

public class Anotações {

}
